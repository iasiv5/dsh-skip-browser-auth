# Lockfile 可移植性：本仓库所有 npm 操作固定使用公共源，保证 package-lock.json
# 的 resolved URL 始终是 registry.npmjs.org，可被 GitHub Actions 等
# 无法解析腾讯云内网镜像域名的环境 npm ci（2026-09-10 v0.2.0 发布失败教训，
# 同类修复见 a196865）。发布源另见 package.json publishConfig.registry。
registry=https://registry.npmjs.org/
